.DS_Store
node_modules
/build
/.svelte-kit
/package

# For now we don't need to ignore the .env file because the repo is private; saves headaches
# TODO: (SECURITY) uncomment this when we make the repo public
.env
.env.*

!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
